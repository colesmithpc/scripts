# 🖥️ System Monitor (Python + psutil)

A lightweight, cross-platform **system monitoring tool** written in Python using `psutil`.  
It displays real-time information about CPU, memory, disk usage, network I/O, and system details — refreshing every 5 seconds.

---

## 🚀 Features

- Live **CPU usage**, core/thread count, and frequency  
- **Memory & Swap** usage breakdown  
- Detailed **disk partition** usage  
- **Network I/O** (sent/received)  
- Cross-platform (Windows, Linux, macOS)  
- Auto-refreshing TUI-style interface  
- Uses `psutil` for accurate system metrics  

---

## 📦 Requirements

Install dependencies:

```bash
pip install psutil
